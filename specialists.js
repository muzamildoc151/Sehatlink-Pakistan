import{createClient}from"https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm";import{SUPABASE_URL,SUPABASE_ANON_KEY}from"./config.js";const list=document.getElementById("list");
async function load(){if(!SUPABASE_URL||!SUPABASE_ANON_KEY){list.innerHTML="<div class='alert'>Configure config.js first.</div>";return}const s=createClient(SUPABASE_URL,SUPABASE_ANON_KEY);let q=s.from("specialists").select("name,specialty,city,clinic_name,phone").eq("verified",true).eq("is_demo",false).order("name");const term=document.getElementById("search").value.trim(),sp=document.getElementById("sp").value;if(term)q=q.or(`name.ilike.%${term}%,specialty.ilike.%${term}%,city.ilike.%${term}%`);if(sp)q=q.eq("specialty",sp);const{data,error}=await q.limit(100);if(error){list.innerHTML="<div class='alert'>"+error.message+"</div>";return}list.innerHTML=data?.length?data.map(x=>`<article class="directory-card"><small>VERIFIED</small><h3>${safe(x.name)}</h3><b>${safe(x.specialty)}</b><p>${safe(x.city)} · ${safe(x.clinic_name||"")}</p><p>${safe(x.phone||"")}</p></article>`).join(""):"<p>No verified consultants found yet.</p>"}function safe(x){return String(x??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));}document.getElementById("search").oninput=load;document.getElementById("sp").onchange=load;load();